<h1>Site_detail creation</h1>

<form action="{{ path('site_detail_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('site_detail') }}">
            Back to the list
        </a>
    </li>
</ul>
