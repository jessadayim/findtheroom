<h1>Roomtype list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Room_typename</th>
            <th>Deleted</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('roomtype_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.roomtypename }}</td>
            <td>{{ entity.deleted }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('roomtype_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('roomtype_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('roomtype_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
