<h1>Facility2site list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Building_site_id</th>
            <th>Facilitylist_id</th>
            <th>Deleted</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('facility2site_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.buildingsiteid }}</td>
            <td>{{ entity.facilitylistid }}</td>
            <td>{{ entity.deleted }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('facility2site_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('facility2site_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('facility2site_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
