<h1>Nearly_location creation</h1>

<form action="{{ path('nearly_location_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('nearly_location') }}">
            Back to the list
        </a>
    </li>
</ul>
