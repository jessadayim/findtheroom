<h1>Nearly2site creation</h1>

<form action="{{ path('nearly2site_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('nearly2site') }}">
            Back to the list
        </a>
    </li>
</ul>
