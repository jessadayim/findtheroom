<h1>Pay_type list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Typename</th>
            <th>Deleted</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('pay_type_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.typename }}</td>
            <td>{{ entity.deleted }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('pay_type_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pay_type_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('pay_type_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
