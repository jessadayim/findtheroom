<h1>Roomtype2site list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Roomtype_id</th>
            <th>Building_site_id</th>
            <th>Room_size</th>
            <th>Room_price</th>
            <th>Deleted</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('roomtype2site_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.roomtypeid }}</td>
            <td>{{ entity.buildingsiteid }}</td>
            <td>{{ entity.roomsize }}</td>
            <td>{{ entity.roomprice }}</td>
            <td>{{ entity.deleted }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('roomtype2site_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('roomtype2site_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('roomtype2site_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
