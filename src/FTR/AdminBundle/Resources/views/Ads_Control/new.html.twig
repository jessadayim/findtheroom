<h1>Ads_Control creation</h1>

<form action="{{ path('ads_control_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('ads_control') }}">
            Back to the list
        </a>
    </li>
</ul>
